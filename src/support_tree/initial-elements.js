import React from 'react';
import { MarkerType, Position } from 'reactflow';

export const nodes = [
  {
    id: "placeholder-1",
    type: "input",
    data: { label: "No pattern found" },
    position: { x: 250, y: 0 },
    style: { border: "1px dashed #999", color: "#999", backgroundColor: "rgba(0,0,0,0)" }
  }
];

export const edges = [];
